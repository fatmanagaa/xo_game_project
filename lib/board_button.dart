import 'package:flutter/material.dart';

class BoardButton extends StatelessWidget {
  const BoardButton({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(
        //child: Image.asset('assets/images/O.png'),
      ),
    );
  }
}
